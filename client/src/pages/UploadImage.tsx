import React from "react";
import { Container } from "@mui/material";

const UploadImage = (): JSX.Element => {
  return <Container>Welcome to Upload Image!!!</Container>;
};

export default UploadImage;
